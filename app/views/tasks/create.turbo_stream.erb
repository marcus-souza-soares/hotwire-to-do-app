<%= turbo_stream.append "tasks", partial: "tasks/task", locals: { task: @task } %>
<%# <%= turbo_stream.replace "task_form", partial: "tasks/form", locals: { task: Task.new } %> %>
<%= turbo_stream.replace "task_form", "<a href=\"#{tasks}\">", locals: { task: Task.new } %>
